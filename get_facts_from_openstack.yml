- name: Collect facts from Openstack instances
  hosts: all
  vars:
  tasks:
  # Gather facts about all servers named C<web*>:
  - os_server_facts:
      server: *
    register: openstack_instances
  - debug:
      var: openstack_instances
